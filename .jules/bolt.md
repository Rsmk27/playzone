## 2024-06-09 - Ensure tests are accurate to code functionality | **Learning:** Writing tests that do not accurately mirror the underlying code functionality causes CI failures. In this case, writing error-case tests for validations that didn't exist in the tested code resulted in failing tests. | **Action:** Always verify test assertions against the actual code being tested. Avoid adding tests for non-existent functionality without also adding the implementation code.
