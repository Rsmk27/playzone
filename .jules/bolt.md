## 2024-06-27 - Test Mongoose connection caching logic | **Learning:** When unit-testing module-level state caching (like globalThis.mongoose patterns), utilize vi.resetModules() combined with dynamic imports to reliably reset the module state between tests, and use `// @vitest-environment node` when you need node semantics without overriding the global `vitest.config.ts` environment setting (e.g. `jsdom`). | **Action:** Always use vi.resetModules() when testing module initialization code to avoid side-effects.

## 2024-07-21 - Add missing error and edge case tests for getUserByClerkId | **Learning:** When adding tests for database interactions via vitest, remember to mock all necessary methods of the model (like `findOne`) and assert that they are called with the correct parameters, while utilizing `vi.spyOn(console, 'error').mockImplementation(() => {})` to keep test outputs clean when verifying expected error throws. | **Action:** Always assert the exact error thrown in catch blocks and ensure console logs are gracefully mocked when testing error paths.
